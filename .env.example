# Public origin of the deployed portfolio, e.g. https://example.com
# Leave unset in development. Set at deploy time once the real domain is confirmed.
# When unset, no canonical URL is emitted and metadataBase falls back to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=
